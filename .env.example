# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=notes_world
POSTGRES_USER=notes_world
POSTGRES_PASSWORD=changeme

# Server
PORT=3001
NODE_ENV=development

# Phase 1 single-user constant
# In Phase 4 this becomes dynamic per authenticated user
PHASE1_USER_ID=00000000-0000-0000-0000-000000000001

# Backup timezone (used by db-backup container)
TZ=UTC
